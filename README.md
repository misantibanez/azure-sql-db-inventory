# azure-sql-db-inventory
🔍 Bash script to inventory Azure SQL Databases across all servers and resource groups, using Azure CLI. Displays service tier, status, and zone redundancy in a readable table format.
